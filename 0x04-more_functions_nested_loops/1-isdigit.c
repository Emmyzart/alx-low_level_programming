#include "main.h"

/**
 * _isdigit - entdy point of the program
 * @c: variable
 }* Return: return 1 if successful, 0 otherwise
 */
int _isdigit(int c)
{
	if (c >= 0 && c <= 9)
		return (1);

	else
		return (0);
}
